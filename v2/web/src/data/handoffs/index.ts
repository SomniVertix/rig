/**
 * Handoff data domain: hooks and types for fetching and managing handoff state.
 * Follows the trails/specs pattern for consistency.
 */

export { useHandoffs, useHandoff, useHandoffConversation } from './live';
export type { HandoffSummary } from './types';
export { queryKeys } from '../queryKeys';
