// Promoted to Stage A — real per-project aggregation over live /specs and
// /expeditions data (see live.ts). The project *list* itself is still a
// manual stopgap (GAPS.md §4); everything else here is real.
export * from './live';
export * from './types';
