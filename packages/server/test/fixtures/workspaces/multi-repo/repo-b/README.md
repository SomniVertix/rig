# repo-b

Fixture repository used by the multi-repo cross-repo binding integration
test (Story 5). Its only purpose is to exist as a real directory that the
resolver CLI can be spawned from, alongside `repo-a`, under
`multi-repo.code-workspace`.
