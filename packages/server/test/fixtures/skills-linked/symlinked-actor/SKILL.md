---
name: symlinked-actor
---
Fixture skill used only to prove syncKnownActorsFromActorsDirectory follows
symlinked actor entries (the real ~/.claude/rig-actors directory is entirely
symlinks back into ~/.claude/skills/<name>).
